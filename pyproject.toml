[project]
name = "sweforge"
version = "0.1.0"
description = "A small local software-engineering agent harness built on Deep Agents."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "deepagents>=0.7.0",
    "httpx>=0.28.0",
    "langgraph-checkpoint-sqlite>=2.0.0",
    "pyjwt[crypto]>=2.9.0",
]

[project.scripts]
sweforge = "sweforge.cli:main"
sweforge-github-poll = "sweforge.github_poll_cli:main"
sweforge-github-execute = "sweforge.github_execute_cli:main"
sweforge-github-execution = "sweforge.github_execution_cli:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sweforge"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
