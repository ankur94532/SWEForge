[project]
name = "sweforge"
version = "0.1.0"
description = "A small local software-engineering agent harness built on Deep Agents."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "deepagents>=0.7.0",
    "httpx>=0.28.0",
    "langgraph-checkpoint-sqlite>=3.1.1",
    "langchain-mcp-adapters>=0.3.0",
    "pyjwt[crypto]>=2.9.0",
]

[project.scripts]
sweforge = "sweforge.cli:main"
sweforge-github-poll = "sweforge.github_poll_cli:main"
sweforge-github-workflow = "sweforge.workflow_cli:main"
sweforge-github-execution = "sweforge.github_execution_cli:main"
sweforge-github-publish = "sweforge.github_publish_cli:main"
sweforge-serve = "sweforge.server_cli:main"
sweforge-repo-memory = "sweforge.repo_memory_cli:main"
sweforge-repo-skills = "sweforge.skills_cli:main"
sweforge-review-freeze = "sweforge.review_fixture_cli:freeze_main"
sweforge-review-replay = "sweforge.review_fixture_cli:replay_main"

[dependency-groups]
dev = [
    "langchain-openai>=1.6.0",
    "pytest>=8.0",
    # Parallel test execution: the suite is run several times per work chunk,
    # and it is dominated by many independent SQLite/tmpdir tests.
    "pytest-xdist>=3.6",
    "ruff>=0.9",
    # Operator sandbox provider. Strict GitHub execution fails closed without a
    # registered `sweforge.sandbox_backends` entry point, so no live acceptance
    # scenario can run unless this is installed. Dev-only: never in the wheel.
    "sweforge-acceptance-sandbox",
]

[tool.uv.sources]
sweforge-acceptance-sandbox = { path = "acceptance/sandbox", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sweforge"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
